package com.example.tickbox.repository;

import com.example.tickbox.model.GistExport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GistExportRepository extends JpaRepository<GistExport, Long> {
}
